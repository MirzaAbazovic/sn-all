const base_url = 'http://localhost/tmf-api/resourceCatalog/v4';

export const environment = {
  RESOURCE_CATALOG_ENDPOINT: `${base_url}/resourceCatalog`,
  RESOURCE_CATEGORY_ENDPOINT: `${base_url}/resourceCategory`,
  RESOURCE_CANDIDATE_ENDPOINT: `${base_url}/resourceCandidate`,
  RESOURCE_SPECIFICATION_ENDPOINT: `${base_url}/resourceSpecification`,
};
