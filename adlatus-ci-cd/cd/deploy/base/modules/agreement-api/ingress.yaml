apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agreement-api-ingress
  namespace: adl
  annotations:
    cert-manager.io/issuer: letsencrypt
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  tls:
    - hosts:
      - agreement-api.adl.env.dev.bitconex.com
      secretName: tls-secret
  rules:
    - host: agreement-api.adl.env.dev.bitconex.com
      http:
        paths:
          - path: /?(.*)
            pathType: Prefix
            backend:
              service:
                name: agreement-api-service-test
                port:
                  number: 80
